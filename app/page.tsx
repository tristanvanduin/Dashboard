import { TodayFeed } from "@/components/today/today-feed";

// De homepage is nu de "Vandaag"-triagecockpit: wat heeft vandaag mijn aandacht nodig?
// Het klassieke portfolio-scorebord (hoe deden we het?) staat op /portfolio.
export default function HomePage() {
  return <TodayFeed />;
}
